import React from "react";

function SubmitBtn() {
  return (
    <input
      type="submit"
      className="btn btn-primary"
      value="Search"
      //   @click.prevent="handleSearch"
      //   v-else
    />
  );
}

export default SubmitBtn;
